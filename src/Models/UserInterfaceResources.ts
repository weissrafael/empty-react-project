export enum PagesEnum {
  inbox = 'inbox',
  archived = 'archived',
  login = 'login',
  chat = 'chat',
}

export enum VariantEnum {
  primary = 'primary',
  secondary = 'secondary',
}

export type Variant = 'primary' | 'secondary';
